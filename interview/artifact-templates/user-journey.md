# User Journey Flows: {ProjectName}

Visualizations of primary user journeys extracted during ACIS initialization.

---

## Primary Persona: {PrimaryPersonaName}

### Daily Journey

```mermaid
journey
    title {PrimaryPersonaName}'s Daily Journey with {ProjectName}
    section Before {ProjectName}
      {pain_point_activity_1}: 2: {PrimaryPersonaName}
      {pain_point_activity_2}: 3: {PrimaryPersonaName}
    section Using {ProjectName}
      {key_action_1}: 7: {PrimaryPersonaName}
      {key_action_2}: 8: {PrimaryPersonaName}
      {key_action_3}: 6: {PrimaryPersonaName}
    section Outcome
      {positive_outcome}: 9: {PrimaryPersonaName}
```

### Key Flow: {primary_flow_name}

```mermaid
flowchart TD
    A[{start_state}] --> B{Check: {condition}}
    B -->|Yes| C[{action_if_yes}]
    B -->|No| D[{action_if_no}]
    C --> E[{next_step}]
    D --> E
    E --> F[{end_state}]

    style A fill:#e1f5fe
    style F fill:#c8e6c9
```

### Error/Recovery Flow

```mermaid
flowchart TD
    A[Normal Operation] --> B{Error Detected?}
    B -->|No| A
    B -->|Yes| C[Show Error State]
    C --> D{User Action?}
    D -->|Retry| A
    D -->|Cancel| E[Return to Safe State]
    D -->|Help| F[Show Guidance]
    F --> D

    style C fill:#ffcdd2
    style E fill:#c8e6c9
```

---

## Secondary Persona: {SecondaryPersonaName}

### Interaction Journey

```mermaid
journey
    title {SecondaryPersonaName}'s Interaction with {ProjectName}
    section Trigger
      {trigger_event}: 5: {SecondaryPersonaName}
    section Interaction
      {interaction_1}: 6: {SecondaryPersonaName}
      {interaction_2}: 7: {SecondaryPersonaName}
    section Outcome
      {secondary_outcome}: 8: {SecondaryPersonaName}
```

---

## System State Diagram

```mermaid
stateDiagram-v2
    [*] --> Idle
    Idle --> Active: User opens app
    Active --> Processing: User action
    Processing --> Active: Success
    Processing --> Error: Failure
    Error --> Active: User retry
    Error --> Idle: User exits
    Active --> Idle: User closes app

    state Active {
        [*] --> {default_view}
        {default_view} --> {action_view}: {trigger}
        {action_view} --> {result_view}: Complete
        {result_view} --> {default_view}: Back
    }
```

---

## Data Flow

```mermaid
flowchart LR
    subgraph User Layer
        U[User]
    end

    subgraph App Layer
        UI[UI Components]
        BL[Business Logic]
    end

    subgraph Data Layer
        LS[Local Storage]
        API[Backend API]
    end

    U -->|Input| UI
    UI -->|Actions| BL
    BL -->|Read/Write| LS
    BL -->|Sync| API
    API -->|Response| BL
    BL -->|Update| UI
    UI -->|Display| U
```

---

## Offline Journey (if applicable)

```mermaid
flowchart TD
    A[User Action] --> B{Network Available?}
    B -->|Yes| C[Online Flow]
    B -->|No| D[Offline Flow]

    C --> E[Send to Server]
    E --> F[Update Local]

    D --> G[Store Locally]
    G --> H[Queue for Sync]
    H --> I{Network Restored?}
    I -->|Yes| J[Background Sync]
    I -->|No| H
    J --> F

    F --> K[Update UI]

    style D fill:#fff3e0
    style G fill:#fff3e0
    style H fill:#fff3e0
```

---

## Critical Path Identification

| Journey Step | Critical? | Failure Impact | Recovery |
|--------------|-----------|----------------|----------|
| {step_1} | Yes | {impact} | {recovery} |
| {step_2} | No | {impact} | {recovery} |

---

## Journey Metrics

| Metric | Target | Current | Notes |
|--------|--------|---------|-------|
| Time to value (first use) | {target} | - | |
| Steps to complete {key_action} | {target} | - | |
| Error recovery success rate | {target} | - | |

---

*Generated by ACIS /init on {timestamp}*
*Persona data from: {interview | docs}*
